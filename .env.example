# Base URL of the NestJS REST API this dashboard consumes (backend team owns it).
# Copy to `.env.local` and point at your running API. No secrets live here —
# the only client credential is the user's own session JWT, obtained at login.
VITE_API_URL=http://localhost:3000
